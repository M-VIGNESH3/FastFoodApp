apiVersion: v1
kind: Service
metadata:
  name: user-service-service
  namespace: {{ .Values.global.namespace | default "fastfood" }}
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: 5001
  selector:
    app: user-service
