apiVersion: v1
kind: Service
metadata:
  name: order-service-service
  namespace: {{ .Values.global.namespace | default "fastfood" }}
spec:
  type: ClusterIP
  ports:
    - port: 5003
      targetPort: 5003
  selector:
    app: order-service
