# Frontend - Service
# Kind: Service
# Type: LoadBalancer (or NodePort for practice)
# Port: 80 -> 80

apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: fastfood
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: frontend